@model IEnumerable<THIDinhVu_61131562.Models.QuanTri>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>TÀI KHOẢN HỆ THỐNG</h2>

<p>
    @Html.ActionLink("Đăng ký tài khoản", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Admin)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HoTen)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Password)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Admin)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HoTen)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Password)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.Email }) |
            @Html.ActionLink("Xem chi tiết", "Details", new { id=item.Email }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.Email })
        </td>
    </tr>
}

</table>
